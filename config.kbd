(defcfg
  process-unmapped-keys yes
  concurrent-tap-hold yes
  ;; sequence-input-mode visible-backspaced

  ;; linux-dev /dev/input/by-id/usb-Kinesis_Kinesis_Adv360_360555127546-if01-event-kbd
  ;; linux-dev /dev/input/event4

  danger-enable-cmd yes
  rapid-event-delay 5     ;; Changed from 0 to 5 to help with laptop debounce
  chords-v2-min-idle 30   ;; Higher idle protects fast typing
)

(defsrc
        q  w  e  r  t       y  u  i  o  p
  caps   a  s  d  f  g       h  j  k  l  ;
          z  x  c  v  b       n  m  ,  .  /  
)

(defvar
  chord_timeout      35
  tap_timeout        170
  hold_timeout       150
  one_shot_timeout   500 ;; dismiss after that time
)

(defalias
  ;; ONE SHOT
  os_go         (one-shot-press $one_shot_timeout (layer-while-held L_GO))
  os_media      (one-shot-press $one_shot_timeout (layer-while-held L_MEDIA))
  os_nums       (one-shot-press $one_shot_timeout (layer-while-held L_NUMS))
  os_ff         (one-shot-press $one_shot_timeout (layer-while-held L_FF))
  os_ch         (one-shot-press $one_shot_timeout (layer-while-held L_CHARS))
  os_mods       (one-shot-press $one_shot_timeout (layer-while-held L_MODS))

  ;; WHILE HELD (ALPHA)
  al_nav_a          (tap-hold $tap_timeout $hold_timeout a (layer-while-held L_NAV_R))
  al_nav_semicolon  (tap-hold $tap_timeout $hold_timeout ; (layer-while-held L_NAV_L))
)

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;; LAYERS ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

;; --------------------------------------------------------------
;; Alpha/Plain/Chars chords
(include _alpha.kbd)

;; --------------------------------------------------------------
;; Navigation chords
(include _nav.kbd)

;; --------------------------------------------------------------
;; Number chords
(include _nums.kbd)

;; --------------------------------------------------------------
;; Function chords
(include _ff.kbd)

;; --------------------------------------------------------------
;; Media layer
(include _media.kbd)

;; --------------------------------------------------------------
;; Go-to shortcuts layer
(include _go.kbd)

;; --------------------------------------------------------------
;; Modifiers layer
(include _mods.kbd)
